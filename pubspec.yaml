name: flutter_redux_example
description: Flutter with Redux - example mobile application.

version: 0.0.1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^3.1.0
  equatable: ^0.4.0
  dio: ^2.1.13
  redux: ^3.0.0
  redux_thunk: ^0.2.1
  flutter_redux: ^0.5.3
  shared_preferences: ^0.5.3+4
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
