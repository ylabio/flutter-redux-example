import 'package:flutter/material.dart';

class Styles {
  static const baseColor = Colors.black;
  static const secondaryColor = Colors.white;
  static const nightBlack = Color(0xFF3F3F3F);

  static const itemListTitle =
      TextStyle(fontSize: 16, fontWeight: FontWeight.w500);

  static const title = TextStyle(fontSize: 28, fontWeight: FontWeight.bold);
  static const content =
      TextStyle(fontSize: 18, height: 1.5, fontWeight: FontWeight.normal);
}
